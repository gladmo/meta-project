# 架构

English | [中文](architecture.zh.md)

{{PROJECT_NAME}} 的有序地图：仓库如何组合、有哪些模块、行为在哪里扩展。决策依据存放在 [Agent Note](../.agents/notes/README.md)；模块细节存放在[模块页](modules/README.md)。

<!-- TODO(template): 本页是有序地图而非穷举参考。读者读完后应当掌握系统的形状和用于检索细节的词汇。 -->

## 总览

<!-- TODO(template): 用两三句话说明系统的类别、主要运行时，以及解释其形状的那一个核心设计思想。 -->

## 模块地图

<!-- TODO(template): 每个模块一行——名称、职责、依赖什么。按分层排序：基础在前，入口在后。链接每个模块的归属页面或 README。 -->

```
{{MODULE_A}}    {{一行职责}}
{{MODULE_B}}    {{一行职责}}
```

## 核心流程

<!-- TODO(template): 在模块地图之上按有序步骤描述每个具名流程——请求生命周期、启动、关闭、错误传播。只保留顺序与交接点；细节归属模块页。 -->

## 扩展点

<!-- TODO(template): 列出每个可以在不改动核心流程的前提下增加行为的文档化位置：钩子、插件、注册表、配置。每项写明契约、归属方以及消费者在哪里注册。 -->

## 不变量

<!-- TODO(template): 陈述系统在各模块之间保证的关系——资源所有权、顺序、可见性。能够被两个模块独立观察到的不变量，适合做成带独立测试的运行时检查。 -->
